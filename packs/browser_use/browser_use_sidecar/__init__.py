"""browser_use sidecar — FastAPI app exposing the browser-driving API.

Mounted into the ``browser-use`` container at ``/opt/pack/browser_use_sidecar``
with ``PYTHONPATH=/opt/pack``. Started by the Dockerfile's uvicorn CMD.
"""
