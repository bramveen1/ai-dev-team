# GitHub fine-grained PAT for the aidt-dispatch machine user (worker pool identity).
# Replace the line below with the real token (no spaces, no quotes).
# Perms: Contents RW, Pull requests RW, Workflows RW, Metadata R — scoped to bramveen1/* only.
# REPLACE_WITH_REAL_TOKEN
