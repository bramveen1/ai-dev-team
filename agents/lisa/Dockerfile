FROM ai-dev-team-base:latest

# Lisa uses the base image only (no additional dependencies for Phase 1)
# MCP server configs will be added in a later phase

# Keep container running so the router can docker exec into it
CMD ["sleep", "infinity"]
