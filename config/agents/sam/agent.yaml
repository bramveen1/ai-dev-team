# Sam — agent manifest. See config/agents/lisa/agent.yaml for schema.

name: Sam
container: sam
thinking_status: "is digging in…"

# Sam's tools come from packs (see packs/<name>/). The github pack
# loads packs/github/prompt.md into his system prompt, injects
# GITHUB_TOKEN at dispatch time, and routes merges through the
# Slack approval card. PR 4 (#84) shipped the pack itself; this
# entry is what actually opts Sam in.
packs:
  - github
